<?php
{% if namespace %}
namespace {{ namespace }};
{% endif %}

use Zenith\Service;
{% if methods %}
use Zenith\SOAP\Request;
use Zenith\SOAP\Response;
{% endif %}

class {{ classname }} extends Service {
{% if methods %}
{% if methods|length > 1 %}{% set spacer = "\n\t" %}{% else %}{% set spacer = "" %}{% endif %}
	{% for i, method in methods %}public function {{ method }}(Request $request, Response $response) {
	}
{% if i != methods|length - 1 %}{{ spacer }}{% endif %}{% endfor %}
{% endif %}}
?>